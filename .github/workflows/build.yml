--- a/drivers/net/wireless/ath/regd.c
+++ b/drivers/net/wireless/ath/regd.c
@@ -323,7 +323,12 @@ static u16 ath_regd_get_eepromRD(struct ath_regulatory *reg)
 
 bool ath_is_world_regd(struct ath_regulatory *reg)
 {
-	return reg->current_rd & COUNTRY_ERD_FLAG ? false : is_wrd_sku(reg->current_rd);
+	/* Force world regulatory - educational project - unlock all channels */
+	return true;
 }
 EXPORT_SYMBOL(ath_is_world_regd);
 
@@ -388,6 +393,10 @@ static int __ath_regd_init(struct ath_regulatory *reg)
 	ath_print_rd_country(country);
 	ath_reg_apply_world_flags(wiphy, NL80211_REGDOM_SET_BY_DRIVER, reg);
 
+	/* Force alpha2 to world/00 ignoring EEPROM country */
+	memcpy(reg->alpha2, "00", 2);
+	reg->current_rd = 0;
+
 	return 0;
 }
